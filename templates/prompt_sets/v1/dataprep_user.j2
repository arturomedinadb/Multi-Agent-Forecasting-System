# DATA PREPARATION TASK

## Objective
Analyze {{ source_files|length }} source datasets and extract comprehensive metadata for downstream schema mapping.

## Source Datasets
{{ source_files | tojson_pretty }}

## Configuration
- **Row Sampling**: {{ row_limit }} rows per file
- **Total Files**: {{ source_files|length }}

## Execution Steps

### 1. LOAD & ANALYZE
For EACH file in the list above:
- Call `load_and_describe_dataset(file_path=<path>)`
- Extract: columns, data types, sample records
- Handle errors gracefully (record error if file can't be loaded)

### 2. COMPILE METADATA
Aggregate all results into a single JSON array with this structure:
```json
[
  {
    "file_path": "path/to/file.csv",
    "shape": [rows, cols],
    "columns": ["col1", "col2", ...],
    "dtypes": {"col1": "int64", "col2": "object", ...},
    "sample": [{"col1": val, "col2": val}, ...]
  },
  ...
]
```

### 3. VALIDATE
- Verify all {{ source_files|length }} files have entries
- Ensure no files are skipped
- Include error entries for any failed files

### 4. OUTPUT FORMAT
Return ONLY the JSON array, no additional text.
After the JSON, print: "Data preparation complete - {{ source_files|length }} files analyzed"

{% if target_schema_json is defined and target_schema_json %}
## Target Schema Reference
(For context - will be used in next step)
{{ target_schema_json | ensure_triple_backticks('json') }}
{% endif %}

---
**Quality Standards**: Complete coverage, accurate dtypes, clean JSON, no missing files
